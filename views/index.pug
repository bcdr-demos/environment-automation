extends layout

block content
  h1 Coder 1.14 Environments are Smart...
  ul
    li 
      strong Environment Name: 
      span #{env.CODER_ENVIRONMENT_NAME}
    li 
      strong Image URI: 
      span #{env.CODER_IMAGE_URI}
    li 
      strong Max Memory: 
      span #{env.CODER_MEMORY_LIMIT} GB
    li 
      strong Max CPU Cores: 
      span #{env.CODER_CPU_LIMIT}
    hr
    li 
      strong Your username: 
      span #{env.CODER_USERNAME}
    li 
      strong Your email: 
      span #{env.CODER_USER_EMAIL} 
      a(href="#", onclick="alert(\"You could hook this up with some internal API, idk\");") (Sign up for xyz)
    hr
    input(placeholder="Environment name")
    | 
    select
      each image, index in images
        option(value=image.id)= image.repository
    | 
    button(onclick="alert(\"This doesn't work, but it could work very easily by running another coder-cli script with exec(): \\n\\n$ coder envs create [name] --image [image]\")") Create new environment
    br
    br
    strong Run command on environent:
    | 
    select
      each workspace, index in workspaces
        option(value=workspace.id)= workspace.name
    | 
    button(onclick="alert(\"This doesn't work, but it could work very easily by running another coder-cli with exec(): \\n\\n$ coder sh env-name ping google.com\")") Execute

